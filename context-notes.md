# 결정 기록

작업 중 갈림길에서 무엇을 왜 골랐는지 적는다. 코드를 읽어서 알 수 있는 것은 적지 않는다.

## 2026-09-06 채점을 순수 함수로 분리

`src/scoring.js` 는 DOM 도 게임 상태도 모른다. 이 게임의 재미가 전부 채점에 걸려 있어
자동 테스트로 지킬 대상이 여기뿐이기 때문이다. 화면과 얽히면 테스트가 불가능해진다.

## 2026-09-06 테스트 스크립트에 glob 을 쓴다

`node --test test/` 는 Windows 에서 디렉터리를 모듈로 해석해 `Cannot find module` 로 죽는다.
`node --test test/*.js` 로 파일을 직접 넘긴다. 계획 문서에는 디렉터리 형태로 적혀 있으나
동작하지 않으므로 이 쪽이 정본이다.

## 2026-09-06 없는 축의 가중치를 재분배한다

계량 재료가 없는 주문에서 계량 축(25%)을 0점으로 두면 완벽하게 만들어도 75점이라
별 3개가 절대 나오지 않는다. 축이 존재하지 않으면(`null`) 가중치를 남은 축에
비례 배분해 총점 만점이 100점이 되도록 한다.

## 2026-09-06 이중 감점을 하지 않는다

재료를 빠뜨리면 재료 축에서 이미 감점된다. 순서 축과 계량 축은 "담긴 것들"만 보고
빠진 것을 다시 벌하지 않는다. 그러지 않으면 실수 하나가 세 축을 동시에 깎아
별점이 필요 이상으로 가혹해진다.

## 2026-09-06 밸런스 수치는 그대로 둔다

주문 200개 시뮬레이션 결과, 성실한 플레이어는 200/200 별 3개(기대와 일치), 덜렁대는
플레이어(계량 1.25배 + 마지막 재료 누락)는 별 1개 55회·별 2개 145회·별 3개 0회로
"대충 만들면 별 3개는 거의 불가능"이라는 기대와 맞는다. 다만 첫 해금(250원)까지 걸리는
주문 수는 1개로, brief 가 기대한 "서너 주문"보다 훨씬 빠르다. 시작 재료로 만들 수 있는
가장 싼 주문도 원가 250원 안팎이라 `PRICE_MARKUP`(2.2배) 적용 시 첫 정산부터 250원을
넘기기 때문이다. 이 항목은 브리프가 상수 조정 여부를 컨트롤러 판단에 맡겼으므로
`AMOUNT_TOLERANCE`/`STAR_THRESHOLDS`/`PRICE_MARKUP` 어느 것도 건드리지 않고 수치만
보고한다.

## 2026-09-06 잠긴 재료 해금 비용을 10배로 올린다

위 시뮬레이션에서 첫 해금까지 1주문이 걸린다는 게 확인되자 컨트롤러가 원인을
`unlockCost` 로 지목했다. 완벽한 주문 하나의 정산액이 550~1470원인데 첫 해금가는
250원이라 해금 진행이 한 주문 만에 끝나 성장 루프가 없었다. `src/data.js` 의 잠긴
재료 7종의 `unlockCost` 를 전부 10배로 올렸다(250→2500 ... 900→9000). 시작 재료
5종의 `unlockCost: 0` 은 그대로다. `PRICE_MARKUP` 을 낮추는 대신 `unlockCost` 를
올린 이유는 정가가 주문서에 그대로 노출되는 값이라 2.2배 마크업은 자연스럽지만
줄이면 가격이 이상하게 보이기 때문이다. 돈이 모이는 대로 가장 싼 잠긴 재료부터
사는 플레이어를 시뮬레이션하면 첫 해금까지 3주문, 전체 12종 해금까지 26주문이
걸린다.
